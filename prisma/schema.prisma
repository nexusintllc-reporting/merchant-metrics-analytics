generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Session {
  id            String    @id
  shop          String
  state         String
  isOnline      Boolean   @default(false)
  scope         String?
  expires       DateTime?
  accessToken   String
  userId        BigInt?
  firstName     String?
  lastName      String?
  email         String?
  accountOwner  Boolean   @default(false)
  locale        String?
  collaborator  Boolean?  @default(false)
  emailVerified Boolean?  @default(false)
}

model StoreEmailSettings {
  id              String   @id @default(cuid())
  shop            String   @unique
  fromEmail       String
  fromName        String
  enabled         Boolean  @default(true)
  additionalEmails String[] // NEW: Add this line for multiple email addresses
  sendGridApiKey  String?
  scheduleEnabled Boolean  @default(false)
  scheduleTime    String   @default("09:00")
  timezone        String   @default("UTC")
  accessToken     String?
  lastSent        DateTime?  // ADD THIS FIELD
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@map("store_email_settings")
}

// ADD THIS NEW MODEL
model EmailSentLog {
  id           String   @id @default(cuid())
  shop         String
  scheduleTime String
  sentDate     DateTime
  createdAt    DateTime @default(now())

  @@index([shop, sentDate])
  @@unique([shop, scheduleTime, sentDate])
  @@map("email_sent_log")
}